#include <stdlib.h>
#include <time.h>
/**
 * main -Entry point
 *
 * Return: Always 0
 */
 
int main(void)
{
		int n;

			srand(time(0));
				n = rand() - RAND_MAX / 2;
					if n(n > 0)
						printf("%d is positive\n");
					else if (n = 0)
						printf("%d is positive\n");
					else if (n < 0)
						printf("%d id negative\n");
					return (0);
}
